# MCDH v1.2.0

本次更新根据 `v1.1.0..4f3d9e5` 的 12 条 Git 提交整理，重点是 MCDK 游戏启动、应用内更新和自定义导出。

## 重点更新

### 使用 MCDK 启动游戏

- 内置 MCDK，点击组件卡片右下角播放按钮，即可以组件根目录为工作目录，在独立控制台中启动游戏，无需配置系统 PATH。
- 首次没有 `.mcdev.json` 时，由 MCDK 引导选择游戏程序并完成配置；已有项目配置不会被 MCDH 改写。游戏本体需自行安装。
- 同一 MCDH 数据目录一次管理一个 MCDK 会话；关闭 MCDH 后游戏继续运行，重新打开应用可校验并恢复会话状态。
- 运行中的组件禁止通过 MCDH 删除、移动、重生 UUID、提升清单版本或写入 MCS 配置，避免干扰游戏会话。

### MCDK 自动更新与校验

- 默认在 MCDH 启动时后台检查 MCDK 正式版本并自动下载，可在“设置 > 开发工具 > MCDK”查看版本、更新状态、失败原因，或关闭自动更新、手动检查更新。
- 下载后校验官方资产摘要、大小和 Windows x64 PE 格式，验证成功后才切换版本。
- 使用独立版本目录，不覆盖运行中的程序；更新失败保留原版本，文件损坏时优先回退上一版，再使用内置副本。
- 构建流程加入锁定版本的 MCDK 资源准备、校验和离线缓存复用，并补充验证说明。

### MCDH 应用内更新

- 发现新版后，点击“立即更新”即可在应用内下载，完成校验后自动退出旧版、安装并重启，无需手动运行安装包。
- 安装版使用 NSIS 静默更新并保留安装目录；便携版在原目录替换发布文件，不转换成安装版。
- 展示下载、校验与安装状态，只接受本仓库官方 Release 中匹配版本及平台、具备 SHA-256 摘要的更新资产。
- 安装或启动失败时尝试恢复原应用文件并重新打开应用，保留诊断信息，不改动组件库和用户设置。
- MCDH 本体仍需用户点击“立即更新”才开始下载；MCDK 默认后台自动更新，两者设置和流程独立。

### 自定义导出与 MCP

- 新增可保存的自定义导出方案，支持接入本机 EXE 或解释器脚本，按组件类型显示独立导出按钮，不改变内置 ZIP 导出规则。
- 支持实时日志、任务状态、取消进程树，以及产物重名时覆盖、添加后缀或取消。
- 新增五个 MCP 工具：`list_custom_export_profiles`、`start_custom_export`、`get_custom_export_task`、`cancel_custom_export`、`resolve_custom_export_conflict`。
- MCP 只能执行桌面端明确授权的已保存方案，不能修改程序、参数或授权；任务日志通过工具结果返回，不污染协议输出。
- 补充自定义导出接入协议、Python 示例和独立 Windows 测试打包器，方便验证外部程序接入。

## 问题修复

- 修复重新打开导出界面、恢复已完成任务时重复触发完成通知的问题；恢复仍在运行的任务后，完成时继续正常通知。

## 下载与使用说明

- `MCDH-1.2.0-windows-x64-setup.exe`：当前用户安装版，无需管理员权限。
- `MCDH-1.2.0-windows-x64-portable.zip`：解压后运行 `MCDH.exe`，请保留同目录的 `mcdh-mcp.exe` 和 `mcdk` 资源目录。
- 支持 Windows 10/11 x64，需要系统 WebView2 Runtime；启动游戏还需自行安装游戏本体。
- 发行包尚未进行商业代码签名，首次运行可能出现 SmartScreen 提示。请从官方 Release 下载，并核对随实际构建生成的 `SHA256SUMS.txt`。
- 本日志不预填未构建产物的 SHA-256；发布时应上传对应安装包、便携 ZIP 和校验清单。

## 主要提交

- MCDK 集成与验证：`b9c6e9a`、`76f149c`、`c43e58e`、`c5a9fca`。
- 应用内更新与隐私说明：`38d7e08`、`df3846c`。
- 自定义导出、MCP 与测试工具：`d17081a`、`f358791`、`dd964ec`、`cfe0e17`、`31c2806`。
- 导出通知修复：`4f3d9e5`。

---

# MCDH v1.1.0

MCDH v1.1.0 聚焦组件信息管理、完整备份与日常操作效率，并补齐了收藏、更新提示和更可靠的导出流程。

## 重点更新

### 可携带的组件信息

- 每个组件根目录现在可以使用 `.mcdh.json` 保存显示名称、标签和收藏状态。
- 配置支持 JSONC 注释和尾随逗号；修改时会保留已有注释与未知字段。
- 旧组件无需迁移即可继续使用，第一次修改组件信息时才会创建配置文件。
- 配置损坏时不会覆盖原文件，组件仍会使用旧数据正常显示，并在扫描问题中给出提示。

### 收藏与组件管理

- 左侧新增“收藏”分类和数量统计。
- 组件卡片可以直接点击星标收藏或取消收藏。
- 配置面板可以统一修改显示名称、标签和收藏状态。
- 收藏、配置和导出等快捷操作完成后局部更新，不会额外扫描全部组件来源。

### 清洁传输与完整备份

- 保留原有“导出游戏 ZIP”，用于生成不包含开发辅助文件和 `.mcdh.json` 的清洁游戏包。
- 新增“导出完整 ZIP”，保留点号项、MCS 配置、Python 开发文件、空目录和 `.mcdh.json`，适合完整备份或迁移开发环境。
- 导入时新增“完整恢复”选项；默认仍采用安全的清洁导入。
- 复制、新建和导入会正确维护组件元数据；符号链接继续跳过，避免带入组件目录之外的内容。

### 更顺手的导出流程

- 成功导出后会记住上次使用的目录，下次自动填写。
- 目标目录存在同名 ZIP 时，可以选择覆盖原文件、自动添加序号或取消。
- 覆盖时会先完成临时 ZIP，再替换原文件，降低导出中断造成文件损坏的风险。

### 更快地打开目录和 VS Code

- 打开组件目录和 VS Code 改为通过组件索引直接定位，不再在点击时扫描全部来源或递归统计目录大小。
- VS Code 只检查组件根目录中的 `.code-workspace` 文件。
- 桌面端会在后台启动目标程序，不再等待外部进程退出，解决点击后长时间卡顿的问题。

### 更新体验

- MCDH 每次启动会检查 GitHub 上最新的正式 Release；有新版本时显示更新说明和官方下载入口。
- 首次安装或升级后的第一次启动会显示当前版本更新日志，并且只显示一次。
- 网络不可用时会静默跳过检查，不影响任何本地组件管理功能。
- MCDH 不会自动下载或安装更新。

### MCP 与兼容性

- 新增 `set_component_metadata`，可设置组件显示名称、标签和收藏状态。
- 导入和导出的 `content_mode` 支持 `clean` 与 `full`。
- 导出的 `conflict_policy` 支持 `rename`、`overwrite` 与 `error`。
- 原有 `set_component_tags` 和省略新参数的调用方式继续兼容。

## 下载说明

- `MCDH-1.1.0-windows-x64-setup.exe`：当前用户安装版，无需管理员权限。
- `MCDH-1.1.0-windows-x64-portable.zip`：解压后运行 `MCDH.exe`，请保持 `mcdh-mcp.exe` 位于同一目录。
- 支持 Windows 10/11 x64，需要系统 WebView2 Runtime。

本版本尚未进行商业代码签名，Windows 首次运行时可能显示 SmartScreen 提示。请从本仓库官方 Release 下载，并核对下方 SHA-256。

## SHA-256

```text
60bf4e1672b501086f8293fdb6898d4817a65af7564b2881bca1100c4c91b194  MCDH-1.1.0-windows-x64-setup.exe
451f785692c0eb989efb53de1d58f4a224d82092b7b56a9b76b5289118f86714  MCDH-1.1.0-windows-x64-portable.zip
```
